apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rubyhelloworld.fullname" . }}-test-connection"
  labels:
    {{- include "rubyhelloworld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rubyhelloworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
