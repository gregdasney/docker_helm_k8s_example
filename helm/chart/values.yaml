replicaCount: 2
containerPort: 8080
image:
  repository: helloworld
  pullPolicy: Never # Needs to be never for minikube... I think

service:
  type: ClusterIP
  port: 80


ingress: # todo think about this
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

